pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                
                echo "build stage"
                sh 'cd app && docker build -t .'
            }
        }
    }
}

