pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                
                echo "docker"
                sh 'docker pull jenkins/jenkins'
            }
        }
    }
}

